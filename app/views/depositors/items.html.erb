                        <!-- Main page title -->
                        <div class="row-fluid"> 
                            <div class="page-header">
                                    <h1><%= t('items') %> <small><%= t('forsellto') %> <% if @depositor.id == current_user.id -%><%= t('you') %><% else -%><a href="<%= url_for(:controller => "depositors", :action => "show", :depositorid => @depositor.id) %>"><%= @depositor.firstname.capitalize %> <%= @depositor.lastname.capitalize %><% end %></a></small></h1>
                            </div>
                        </div>
                        
                        <%= render "itemslist" %>